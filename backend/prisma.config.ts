/**
 * Prisma Database Configuration Reference
 * Version: 1.0
 *
 * This is a reference file only - NOT used by Prisma CLI
 * Prisma reads config from each schema file's generator and datasource blocks
 *
 * Database URLs (in .env):
 * - DATABASE_URL: Board database
 * - IMAGE_DATABASE_URL: Image database
 *
 * Use npm scripts:
 * - npm run prisma:gen    (generates both clients)
 * - npm run prisma:mig    (migrates both databases)
 * - npm run prisma:push   (pushes both schemas)
 *
 * Schemas:
 * - prisma/board.schema.prisma (Board model)
 * - prisma/image.schema.prisma (Image model)
 */
